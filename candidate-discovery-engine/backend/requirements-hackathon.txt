# Minimal requirements for the hackathon v2 pipeline ONLY.
# Install these in a clean virtual environment to avoid dependency conflicts.
#
# Setup:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements-hackathon.txt
#   python scripts\smoke_test_pipeline.py

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
structlog>=24.4.0
PyMuPDF>=1.25.0
python-docx>=1.1.2
httpx>=0.28.0

# Core matching engine
sentence-transformers>=2.7.0,<3.0.0
rank-bm25>=0.2.2
rapidfuzz>=3.10.0
numpy>=1.26.0,<2.0.0

# Optional: for v1 API (can remove for pure hackathon demo)
# openai>=1.57.0
# azure-search-documents>=11.6.0
# redis[hiredis]>=5.2.0
# sqlalchemy[asyncio]>=2.0.36
# asyncpg>=0.30.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
